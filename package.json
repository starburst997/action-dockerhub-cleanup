{
  "name": "action-dockerhub-cleanup",
  "version": "0.0.1",
  "main": "index.js",
  "license": "MIT",
  "scripts": {
    "package": "export SET NODE_OPTIONS=--openssl-legacy-provider && ncc build"
  },
  "dependencies": {
    "@actions/core": "^1.2.6",
    "axios": "^0.21.1"
  },
  "devDependencies": {
    "@vercel/ncc": "^0.24.1"
  }
}
